= form_for @new_policy do |f|
  - if @new_policy.errors.any?
    #error_explanation
      %h2= "#{pluralize(@new_policy.errors.count, "error")} prohibited this new_policy from being saved:"
      %ul
        - @new_policy.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
