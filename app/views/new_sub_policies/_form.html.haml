= form_for @new_sub_policy do |f|
  - if @new_sub_policy.errors.any?
    #error_explanation
      %h2= "#{pluralize(@new_sub_policy.errors.count, "error")} prohibited this new_sub_policy from being saved:"
      %ul
        - @new_sub_policy.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
